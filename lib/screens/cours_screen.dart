import 'package:flutter/material.dart';

class CoursScreen extends StatelessWidget {
  const CoursScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Page des cours'));
  }
}